pack breaking obsolete | Data pack format has been increased to `117.0`.

advancement | Visible root advancements (that have a `display` not no `parent`) are now required to specify a `background` field. And only root advancements can have a `background` field.

slot-source | Changed slot source `group`: can only use an inline definition in a top-level file.

loot item-modifier | Changed loot function `sequence`: can only use an inline definition in a top-level file.

number-provider breaking | Changed number provider `sum`: renamed field `summands` -> `operands`.

number-provider | Added number providers `product`, `minimum`, `maximum`, and `average`: with field `operands`.

tag | Added block tags `#uncarvable`, `#dangerous_for_teleportation`, `#cat_does_not_teleport_to`, `#enderman_does_not_teleport_to`, `#shulker_does_not_teleport_to`, and `#consumable_does_not_teleport_to`.

tag | Added item tag `#brewing_potion_inputs`.

tag | Added potion tags `#douses_fire`, `#hurts_water_sensitive_entities`, `#extinguishes_entities`, and `#rehydrates_axolotls`.
