pack breaking | Data pack format has been increased to `121.0`.

number-provider | Changed `mod` context float provider: now uses standard modulus instead of floor modulus, matching the int variant.

number-provider | Changed `pow` context float provider: raising 0 to the power of 0 will now cause an error and halt computation, matching the int variant.
