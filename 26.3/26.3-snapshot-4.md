pack breaking obsolete | Data pack format has been increased to `111.0`.

pack breaking obsolete | Resource pack format has been increased to `92.0`.

loot predicate item-modifier advancement number-provider recipe slot-source | Loot table and related folders now have dedicated registry. Most fields now accept a namespaced ID, or inline value. Places that accepted lists now also accept a list of namespaced IDs, a list of inline values, or a has-prefixed tag.

loot predicate item-modifier slot-source breaking | Removed predicate, item modifier, and slot source type `reference`. Instead they can be references by using a namespaced ID string.

command | Changed `/spreadplayers` command. Whether a block is safe to spread a player to is now controlled by the `#entities_can_teleport_to` block tag.

environment-attribute | Added environment attribute `gameplay/natural_mob_spawns`. It is an object with fields:
* `spawns_by_category`: map of spawn category to weighted list of spawn data.
* `spawn_costs`: map of entity type to object with fields `energy_budget` and `charge`.

environment-attribute | Added environment attribute `gameplay/creature_world_gen_spawn_probability`, which is a float between 0 and 1 (left-inclusive). It sets the probability to run an iteration in which mobs defined to spawn in the creature mob category will spawn during world generation. Defaults to `0.1`.

environment-attribute | Changed environment attribute `visual/ambient_particles`:
* Now supports interpolation between timeline keyframes (probabilities will be crossfaded).
* Added new modifier: `append`, which concatenaes all elements with extra layers.

item component | Added data component `cooking_fuel`, with fields `burn_type` (namespaced ID to number provider) and `speed_multiplier` (namespaced ID to number provider).

tag item component breaking | Removed item tag `#brewing_fuel`. Replaced with the `brewing_fuel` data component instead: It has fields `uses` (namespaced ID to number provider) and `speed_multiplier` (namespaced ID to number provider).

component | Added data components `sign_text_front` and `sign_text_back`. The format is the same as `front_text` and `back_text` on sign block entities.

component | Added data component `waxed`, which is a marker for blocks with contents that are waxed.

component | Added data component `cushion/color`, which is applied to cushion entities when they get placed using an item. It can be one of the 16 dye colors.

component | Added data component `villager_food`, representing a items that villagers can eat. It is an object with field `nutrition` (positive integer).

component | Added data component `mob_visibility`, representing the visibility percentage provided by an item with the appropriate `equippable` component, modifying the range at which mobs are able to detect an entity. It is an object with fields `targeting_entity` (entity ID, list of entity IDs, or entity tag) and `visibility` (float between 0 and 10).

block nbt breaking | Changed `sign` and `hanging_sign` block entities. By default, commands and other click events are no longer executed when clicked and text is no longer resolved by default. Added optional boolean `allow_op_features` to restore previous behavior.

block nbt component | Signs will now accept and return the `sign_text_front`, `sign_text_back`, and `waxed` data components.

block nbt breaking | Changed `brewing_stand` block entity:
* Changed `BrewTime` from short -> int.
* Changed `Fuel` from byte -> int.
* Added `total_brew_time` int.
* Added `total_fuel` int.
* Added `speed_multiplier` float.

block nbt breaking | Changed `furnace`, `smoker`, and `blast_furnace` block entities:
* Changed `cooking_time_spent` from short -> int.
* Changed `cooking_total_time` from short -> int.
* Changed `lit_time_remaining` from short -> int.
* Changed `lit_total_time` from short -> int.
* Added `speed_multiplier` float.

advancement loot | Changed advancement rewards. The `loot` field now also accepts an inline value, list of loot table IDs, or a loot table tag.

advancement breaking | Advancement trigger condition fields that accepted an entity predicate or a list of loot conditions are now stricter:
* Entity predicate shorthand format has been removed, instead the `entity_properties` condition has to be explicitly used now.
* A list of conditions is no longer allowed, instead the `all_of` condition has to be explicitly used now.

advancement breaking | Changed `bee_nest_destroyed` advancement trigger:
* Renamed `block` -> `blocks`, now also accepts a list of blocks, or block tag.
* Added `state` field that matches block state properties.

advancement breaking | Changed `brewed_potion` advancement trigger:
* The `potion` field now accepts a `potion_contents` data component (object with `potions` and `effects`).

advancement breaking | Changed `enter_block` and `slide_down_block` advancement triggers:
* Renamed `block` -> `blocks`, now also accepts a list of blocks, or block tag.

advancement breaking | Changed `player_generates_container_loot` advancement trigger:
* Renamed `loot_table` -> `loot_tables`, now also accepts a list of loot table IDs, or loot table tag.

advancement breaking | Changed `crafter_recipe_crafted` and `recipe_crafted` advancement triggers:
* Renamed `recipe_id` -> `recipes`, now also accepts a list of recipe IDs, or recipe tag.

advancement breaking | Changed `recipe_unlocked` advancement trigger:
* Renamed `recipe` -> `recipes`, now also accepts a list of recipe IDs, or recipe tag.

slot-source | Changed slot source `group` (and also the shorthand form):
* `terms`: now also accepts a slot source ID, a list of slot source IDs, or a slot source tag.

slot-source | Changed slot sources `filtered`, `contents`, and `limit_slot`:
* `slot_source`: now also accepts a slot source ID.

loot | The `rolls` and `bonus_rolls` fields in loot table pools now also accept a reference to a number provider.

loot | Added `container_process` loot table type, which takes parameters: `container`, `block_entity`, `block_state`, `origin`.

loot | Changed `advancement_location` loot table type: block entities are now available in context and can be matched by conditions like `match_block`.

loot predicate breaking | Renamed loot table pool field `conditions` -> `condition`, now accepts an inline value or predicate namespaced ID. It no longer includes a short form list, instead `all_of` must be used explicitly.

loot item-modifier breaking | Renamed loot table pool field `functions` -> `modifier`, now accepts an inline value (including short form of `sequence`), or  item modifier namespaced ID. This field is now also available on `alternatives`, `sequence`, and `group` loot entries.

loot breaking | Changed `tag` loot table entry:
* Renamed field `name` -> `items`, now also accepts a single item ID or list of item IDs.

loot | Changed `loot_table` loot table entry:
* Changed `value`: now also accepts a list of loot table IDs, or a loot table tag.
* Added `expand`: working the same as in the `tag` loot table entry.

loot item-modifier breaking | Changed item modifier (loot function) format:
* Renamed `function` -> `type`.
* Renamed `conditions` -> `condition`, now accepts an inline value or namespaced predicate ID. No longer allows a list of conditions, instead `all_of` must be used explicitly.

loot item-modifier breaking obsolete | Changed loot function `set_loot_table`:
* Renamed `map` -> `tag`.

loot item-modifier breaking | Changed loot function `exploration_map`:
* Field `destination` is no longer optional, and now also allows a list of structure IDs, or structure tag.

loot item-modifier breaking | Removed loot function `reference`. Any field that takes a loot function now also accepts an item modifier ID.

loot predicate breaking | Changed predicate (loot condition) format:
* Renamed `condition` -> `type`.
* Removed predicate shorthand list form, instead `all_of` must be used explicitly.

loot predicate breaking | Removed loot condition `reference`. Any field that takes a loot condition now also accepts a predicate ID.

loot predicate breaking | Removed loot condition `block_state_property`. Replaced with loot condition `match_block`, which has the same fields as a block predicate.

loot number-provider breaking | Number providers now always need to specify the `type` field. Previously this defaulted to `uniform`.

recipe breaking | The cooking time in `smoker` and `blast_furnace` recipes should now match the same value as the normal furnace recipe (usually `200`). Cooking time speedup is now handled through the number provider used by the `furnace_fuel` data component.

worldgen breaking | Removed biome fields `spawners`, `spawn_costs`, and `creature_spawn_probability`. Replaced with new environment attributes.

worldgen breaking | Removed feature types `nether_forest_vegetation`, `twisting_vines`, and `weeping_vines`.

worldgen breaking | Changes to noise settings:
* The `final_density` field no longer has the `beardifier` density function implicity added to the result.
* Removed field `aquifers_enabled` and replaced it with optional `aquifers` object.
* Removed field `ore_veins_enabled` and replaced it with optional `ore_vein` list of objects.
* Removed noise router fields `barrier`, `fluid_level_floodedness`, `fluid_level_spread`, `lava`, `vein_toggle`, `vein_ridged`, and `vein_gap`.

worldgen density breaking | Changed density function `constant`: renamed `argument` -> `value`.

worldgen density breaking | Changed density functions `add`, `mul`, `min`, and `max`: renamed `argument1` -> `left` and `argument2` -> `right`.

worldgen density breaking | Changed density functions `abs`, `square`, `cube`, `half_negative`, `quarter_negative`, `squeeze`, `interpolated`, `flat_cache`, `cache_2d`, `cache_once`, `cache_all_in_cell`, and `blend_density`: renamed `argument` -> `input`.

worldgen density breaking | Changed density functions `shift_a`, `shift_b`, and `shift`: renamed `argument` -> `noise`.

worldgen density | Added density function `sub` with fields `left` and `right`.

worldgen density | Added density function `div` with fields `left` and `right`.

worldgen density breaking | Renamed density function `invert` to `reciprocal`.

worldgen density | Added density function `negate` with field `input`.

worldgen density | Added density function `lerp` with density function fields `alpha`, `first`, and `second`.

worldgen density | Added density functions `floor`, `round`, `ceil`, and `truncate` with fields `input` and `multiple` (integer to round to, defaults to 1).

worldgen density | Added density function `beardifier` without fields.

trade breaking | Renamed villager trade field `given_item_modifiers` to `given_item_modifier`. Unlike other fields, this does not support namespaced IDs referencing other files.

tag | Added block tag `#cushion_uses_collision_shape`.

game-test | Removed game test environment `dimension`. Instead each test instance has a `dimension`, which can be any identifier instead of just `overworld`, `nether`, or `end`.
