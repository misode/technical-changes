worldgen | Custom world generation and dimension settings now use the same folder pattern in data packs as other resources. `data/minecraft/<type>/<namespace>/<path>.json` -> `data/<namespace>/<type>/<path>.json`.
