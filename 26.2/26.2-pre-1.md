pack obsolete breaking | Data pack format has been increased to `106.1`.

pack breaking | Resource pack format has been increased to `88.0`.

options | Added `chat-spam-threshold-seconds` and `command-spam-threshold-seconds` server properties, separating the spam thresholds for chat vs. commands.

entity sound | Added `sound_settings` field to entries in the `sulfur_cube_archetype` registry, containing various sound settings for the Sulfur Cube.

command | The `execute on owner` command will now consider vexes as owned by their evoker.

worldgen | Added a new `flat_all_dimensions` world preset. This preset does not show up by default in the create world screen.

assets breaking | Renamed block sprites `block/quartz_pillar.png` and `block/purpur_pillar.png` to append `_side` as a suffix.

assets | Added new entity textures for the Sulfur Cube.

sound breaking | Removed Sulfur Cube sounds `entity.sulfur_cube.hit` and `entity.sulfur_cube.push`.

sound | Added new Sulfur Cube `hit` and `push` sound variants for each archetype.