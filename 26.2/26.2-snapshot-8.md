pack breaking | Data pack format has been increased to `106.0`.

damage-type | Added new `sulfur_cube_hot` damage type.

command | The `publish` command now takes an additional boolean, specifying whether the server should be opened to online multiplayer or just local multiplayer.

command | Added `unpublish` command, which unpublishes the integrated server if one is present.